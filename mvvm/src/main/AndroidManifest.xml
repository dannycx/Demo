<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.danny.databinding">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".DataBindingApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".model.MainActivity"/>
        <activity android:name=".mvvm.view.MVVMActivity"/>
        <activity android:name=".mvvm.view.SimpleActivity"/>
        <activity android:name=".attr.CustomAttrActivity"/>
        <activity android:name=".list.ListViewActivity"/>
        <activity android:name=".attr.NoStaticAttrActivity"/>
        <activity android:name=".fragment.FragmentActivity"/>
        <activity android:name=".recycler.RecyclerViewActivity"/>
        <activity android:name=".collection.ListActivity"/>
    </application>

</manifest>